<Window x:Class="Lab1_Front.SpecificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Спецификация" Height="600" Width="734">

    <DockPanel>

        <!-- Верхняя панель с кнопками -->
        <StackPanel DockPanel.Dock="Top" 
                    Orientation="Horizontal"
                    HorizontalAlignment="Center" 
                    Margin="5">
            <Button Content="Удалить окончательно (Сжать)" 
                    Click="BtnTruncate_Click" 
                    Width="180" Height="30"
                    Margin="5"/>
            <Button x:Name="BtnShowDeleted"
                    Content="Показать удаленные"
                    Click="BtnShowDeleted_Click"
                    Width="150" Height="30"
                    Margin="5"/>
            <Button Content="Восстановить все" 
                    Click="BtnRestore_Click" 
                    Width="150" Height="30"
                    Margin="5"/>
        </StackPanel>

        <!-- Дерево по центру -->
        <TreeView x:Name="SpecTreeView" Margin="5"/>

    </DockPanel>

    <!-- Контекстное меню -->
    <Window.Resources>
        <ContextMenu x:Key="TreeContextMenu" Opened="TreeContextMenu_Opening">
            <MenuItem Header="Добавить" Click="MenuItem_Add_Click"/>
            <MenuItem Header="Изменить" Click="MenuItem_Edit_Click"/>
            <MenuItem x:Name="MenuItemDelete" Header="Удалить" Click="MenuItem_Delete_Click"/>
            <MenuItem x:Name="MenuItemRestore" Header="Восстановить" Click="MenuItem_Restore_Click" Visibility="Collapsed"/>
        </ContextMenu>
    </Window.Resources>
</Window>